// Input: heights = [2, 7, 8, 3, 7, 6]
// Output: 24
// area = l-r * min(l,r);
// console.log((l-r) * Math.min(heights[l],heights[r]));

let heights = [2, 7, 8, 3, 7, 6]
let r=0;
let l=heights.length-1;
let w = 0;




while(r<l){
    w = (l-r) * Math.min(heights[l],heights[r]);
    console.log(r,l,w);
    (heights[r]>heights[l]) ? l--:r++;
}

